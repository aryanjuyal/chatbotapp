
# by requests module
# import requests

# url = "http://127.0.0.1:8000/chat"
# data = {"prompt": "Explain this image to me"}
# files = {"image": open("example.jpg", "rb")}  # optional

# response = requests.post(url, data=data, files=files)
# print(response.json())


# ✅ Option A: Using FastAPI docs

# Open:

# http://127.0.0.1:8000/docs


# Click on /chat → POST → “Try it out”

# Fill in the prompt field

# Optionally upload an image

# Click Execute → You’ll see the JSON response from Gemini


# Your Streamlit code already has the interface — just make it send the POST request to this FastAPI endpoint, instead of calling gemini() directly.

    # Send POST request to FastAPI
        # try:
        #     response = requests.post(url, data=data, files=files)
        #     if response.status_code == 200:
        #         st.success(response.json().get("reply"))
        #     else:
        #         st.error(f"Error {response.status_code}: {response.text}")
        # except Exception as e:
        #     st.error(f"Request failed: {e}")